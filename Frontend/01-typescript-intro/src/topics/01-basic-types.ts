const fullName: string = "Lui";
let hpPoints: number | string = 95;
const lastName: string | 2 = "L";
const isAlive: boolean = true;

hpPoints = "Full";

console.log({
  fullName,
  hpPoints,
  lastName,
  isAlive,
});
